function testElse(val) {
    // Only change code below this line
  
    if (val > 5) {
      return "Bigger than 5";
    } else { //When you use ELSE, IF don't come TRUE, ELSE work
      return "5 or Smaller";
    }
  
    // Only change code above this line

  }
  
  console.log(testElse(4));