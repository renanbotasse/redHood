let myNestedArray = [
    // change code below this line
    ["unshift", false, 1, 2, 3, "complex", "nested"],
    ["loop", "shift", 6, 7, 1000, "method"],
    ["concat", false, true, "spread", "array", ["deep"]],
    ["mutate", 1327.98, "splice", "slice", "push", [["deeper"]]],
    ["iterate", 1.3849, 7, "8.4876", "arbitrary", "depth", [[["deepest"]]]]
    // change code above this line
  ];


  /*
for (i = 1; i < myNestedArray.length; i++) {
myNestedArray[0].unshift(myNestedArray[i])
}

for (x = 1; x < myNestedArray.length; i++) {
    myNestedArray.pop(myNestedArray[x])
}

    let bolovo = [];
    bolovo.unshift(...myNestedArray[0]);

    myNestedArray.pop()
    myNestedArray.unshift(...bolovo)
    const newArray = ["deep", "deeper", "deepest"];

    myNestedArray[1].unshift(newArray[0]);
    myNestedArray[2].unshift(newArray[1]);
    myNestedArray[3].unshift(newArray[2]);

    console.log(myNestedArray)
*/